создаем  две страницы хтмл.
В index.js удаляем все из res.end и проставляем кодировку в res.wtiten

Создаем ссылку с главной странице на с адресом /api/users.
Сервер обрабатывает запрос, создает заголовок (writeHead(HTTP-кодб [строка статуса],{заголовки}).
Создаем объект и котдаем его методу res.end(txt) в JSON формате

