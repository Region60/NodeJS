В файле, в котором мы собираемся делать экспорт  пишем:
module.export = \объект или функция\           // без слешей
В файле куда мы экспортируем пишем:
require ('./путь') // можно сразу присвоить какой-то переменной  let track = require ('./путь')

Каждый модуль НОДА оборачивает в самовызываемую функцию с заданными атрибутами:

(function(require,module,exports,__filename,__dirname) {
\модуль\
})()