
router
 В корне приложения  создаем папку routes, в ней создаем 3 файла в этих файлах задаем требовани и переносим логику
 из файла index.js создаем импорт. В index.js экспортируем наши файлы, и в методе app.use первым параметром
 указываем путь (а в методе get оставляем только слеш), а вторым указываем наш экспорт.
